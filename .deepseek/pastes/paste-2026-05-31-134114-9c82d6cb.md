additional review:Here is the first feature-by-feature review pass, using the current repo/code surfaces I have inspected so far. I am avoiding reduced-scope language and treating this as the full SentinelTwin product.## 0. Cross-cutting issue: root/product orchestrationThis is the biggest issue across every feature.`apps/studio/src/app/page.tsx` is currently acting as product router, workflow launcher, site compiler, draft approver, AI draft controller, scan launcher, JSON import handler, and Studio entrypoint. The file imports dashboard, project launcher, scene builder, scan wizard, guided capture, site intake, site draft review, site compiler, AI draft logic, provider selection, and scene schema in one page-level component. It also owns many independent view booleans:```tsenterStudioshowWizardshowFloorPlanWizardshowSiteIntakeHubshowScanWizardshowGuidedCaptureshowProjectLaunchershowAiDraft```Those are all local root flags, not a product-level view model. ### FeedbackThis is why the app feels confused. The features are real, but the root treats them like overlays/actions instead of product areas.### Needed correctionIntroduce a product-level view state and stop letting every feature become a modal branch. Root should coordinate product views, not contain all workflows.---# 1. Product Home## What exists`StudioDashboardHome.tsx` is a serious file. It already knows about:* current scene* simulation result* saved scenes/projects* workspace modes* project metadata* workspace memory search* organization/account summary* issue severity* DORI quality* scene preview* starter cardsThe top-level nav inside `StudioDashboardHome` currently lists:```tsHomeProjectsReference SitesReportsDocsSettings```but those are currently static nav items with `active: false`, not mature product routes. It also lists workspace modes:```tsCoverageCamera ViewCamera WallPath ReplayCompareReport Lite```as direct mode entries. ## What is goodThe Product Home has the right ingredients:* current site/scene preview* risk/status direction* recent work* quick actions* mode entry points* source labels* simulation-aware labelsThe `ScenePreview` renders walls, doors, windows, privacy zones, critical zones, obstructions, lights, cameras, active path, heatmap cells, and labels. That means Product Home can visually communicate the product value without entering Studio. ## What is poorThe root preview currently has built-in “unfinished app” language:```tsxLoading previewHydrating scene geometry```Those are rendered directly inside `ScenePreview` when `hydrated` is false. That explains the bad first impression on the live page. This is not just a performance issue; it is a product-state issue.Also, Product Home still uses “workspace” heavily:* `Selected Workspace`* `Draft Workspace`* `Your Workspace`* local workspacesThis is wrong for the full product model. The product should distinguish Project, Site, Site Twin, Workspace, Simulation, Evidence, Report.## Needed correctionProduct Home should be reframed around:```textCurrent Site TwinRisk postureOpen findingsRecent Site TwinsRecent ReportsCreate Site TwinOpen Security Twin StudioOpen Audit ReportReference SitesSettings```And the root must stop showing technical hydration states. Use deliberate states:```textBaseline simulation requiredRun baseline simulationCurrent site twin readyNo audit report generated yet```---# 2. Create Site Twin / Site Intake## What exists`SiteIntakeHub.tsx` is already a real product surface. It has source cards for:* Scan Site Photos* Describe with AI* Upload Floor Plan* Import SecurityScene* Build Manually* Verify from FootageThe cards have status, description, output, review requirement, best-for text, steps, limitations, time estimate, confidence, CTA label, and action mapping.  This is good. It means the product direction is present in code.## What is goodThe source taxonomy is strong:* `scan`* `ai_prompt`* `floor_plan`* `json`* `manual`* `camera_evidence`The scan card is correctly recommended and honest: manual-assisted, review required, no automatic segmentation/depth yet. The camera evidence card is also honest: “Preview,” no product-grade video/stream verification yet. ## What is poor`SiteIntakeHub` has its own full sidebar, fake/admin identity block, hardcoded reference baseline image, hardcoded recent site cards, and passive nav.  This makes it feel like a standalone mock surface rather than an integrated product area.Also, it still uses the term `SecurityScene` directly in user-facing areas. For internal schema this is fine, but product UX should say **Site Twin** first and expose `SecurityScene` as a technical/import format.## Needed correctionKeep `SiteIntakeHub`, but integrate it into the product shell:* Use real project/site/nav context.* Replace fake “Admin User / Acme Security” with real current account or remove it from default path.* Replace hardcoded recent sites with actual saved Site Twin/project records.* Product language: “Create Site Twin” → “Review Site Twin Draft” → “Open Security Twin Studio.”* Keep “SecurityScene JSON” only for import/export technical path.---# 3. Scan Site Photos / Guided Capture## What exists`ScanSiteWizard.tsx` has real functionality. It supports:* image upload* multiple photos* supported file validation* active photo* candidate creation* candidate dragging/nudging* candidate status* candidate review* warnings* low-confidence override* auto path creation* compile to scene* operational evidence event* optional simulation after compileThe component creates scan sessions and candidates via `scan-to-scene`, reads local image files, stores dimensions, adds candidates on image click, updates candidate points from pointer movement, compiles the session, records evidence, sets the scene, enters coverage workspace, and conditionally runs simulation.   ## What is goodThis is not empty. It is a real manual-assisted scan flow.The compile guardrails are good:* must have image* blocking errors prevent compile* warnings must be acknowledged* low-confidence candidates require override* missing simulation prerequisites are surfaced ## What is poorThe scan flow currently compiles directly into active scene state:```tssetScene(compiled.scene)setViewMode("map")setWorkspacePreset("coverage")```and then closes. For the full product, this is too direct. Scan output should become a **Site Twin Draft** and pass through `SiteDraftReview` before replacing the active Site Twin.The component also still has `mode?: "manual" | "guided"`, `guidedStep`, and a separate `GuidedCaptureAssistant` elsewhere.  That means scan ownership is unclear.## Needed correctionScan should become:```textSite Intake source→ Scan Site Photos full view→ Candidate marking/review→ Compile to Site Twin Draft→ Site Draft Review→ Approve→ Open Security Twin Studio```Not:```textScan modal→ setScene directly→ close```Also decide ownership:```textScanSiteWizard = full scan shellGuidedCaptureAssistant = internal step/panel```or remove the split from user-facing flow.---# 4. Site Draft Review## What exists`page.tsx` has `siteIntakeSession` and renders `SiteDraftReview` when present. It also has `approveIntakeSession`, calls `approveSiteTwinDraft`, validates approval, promotes the scene, records operational evidence, and conditionally runs baseline simulation. ## What is goodThe correct product safety concept exists:```textcompile/import/draft→ review→ approve→ promote```The code records provenance and evidence during approval. ## What is poorSome flows bypass this and directly set active scene. Scan compile appears to do direct `setScene`. That means draft review is not consistently enforced across source paths.## Needed correctionEvery source must pass through draft review:* scan* AI layout* floor plan* JSON import* manual builder if replacing current site* camera evidence if it mutates scene/camera stateDirect active-scene replacement should be reserved only for explicit “open existing saved Site Twin” or already-approved Site Twin.---# 5. Security Twin Studio## What exists`StudioShell` is real and strong. It renders:* `WorkspaceCanvas`* `CameraWallView`* `CameraViewMode`* `PathReplayView`* `CompareView`* `ReportView`depending on `viewMode`. The shell has:* `TopBar`* `StatusBar`* `LeftPanel`* `ViewModeBar`* `CommandBar`* `DockLayout`* right/left/bottom docks* view settings* autosave recovery* keyboard shortcuts* focus mode* undo/redo* snapshot* simulation* active tools Keyboard shortcuts show tools for:* select* camera* obstruction* light* sensor* path* zone* door/window* wall* measure* comment* report* night mode* snapshot## What is goodStudio is the most mature part of the app.It is not just a canvas. It is a real editor shell with multi-view modes, tools, docks, settings, keyboard controls, report access, and simulation hooks.## What is poorStudio is still conceptually overloaded as “the app.” Product Home, Site Intake, Scan, Reports, Projects, and Settings should surround it as product areas.Also, the mode names still include `Report Lite`, and Product Home points directly to workspace modes. That can make the product feel like a mode switcher rather than a full product.## Needed correctionPosition Studio as:```textSecurity Twin Studio```It should clearly show:* current Project* current Site* current Site Twin* simulation state* report state* evidence/review stateTop bar and root should not blur “workspace” with Site Twin.---# 6. Simulation / Risk Engine## What exists`StudioShell` calls `useSimulation()`, pulls `runSimulation` from store, and binds run simulation to `Ctrl/Cmd + Enter`.  Root `page.tsx` also auto-runs simulation for the seeded scene under certain conditions:```tsif (currentResult || !simulationDirty || scene.source !== "demo") return;runSimulationFromStore();```## What is goodSimulation is wired into both root and Studio.Root also passes `simulationResult`, `simulationDirty`, and `simulationRunning` into `StudioDashboardHome`. ## What is poorThe live root still shows unfinished baseline states. That means either:* auto-run is not fast enough,* hydration exposes bad copy,* result is not cached/precomputed,* or root displays placeholder values too prominently.This is a product readiness problem.## Needed correctionSimulation state needs a clean product contract:```textnot_readybaseline_requiredrunningreadystalefailed```Every surface should map those states consistently.Root should not show raw “loading” fragments. Studio can show more technical status, but Product Home needs product language.---# 7. Camera Operations## What existsCamera Operations are real modes:* `CameraViewMode`* `CameraWallView``StudioShell` renders both from `viewMode`. `page.tsx` also has a `startVerifyFootageFlow` that sets `activeWorkflow` to `verify_footage`, selects a camera, sets camera view verification intent, and opens `camera_view`. ## What is goodThe app is already beyond static planning. It has the beginnings of:```textcamera viewcamera wallcamera evidence / verificationselected camera workflow```## What is poorCamera verification is hidden inside Camera View flow, not yet a first-class Camera Operations path. It may be correct architecturally, but the product needs clearer labels:```textCamera OperationsCamera ViewCamera WallReference Frame VerificationCamera Mismatch Evidence```Also, if there are no cameras, `startVerifyFootageFlow` falls back to coverage workspace and launch notice. That is good, but the product flow should guide the user to add/select a camera rather than just bounce them.## Needed correctionMake Camera Operations a coherent module:```textSelect cameraOpen camera viewOpen camera wallAttach/reference frameVerify camera alignmentRecord evidenceFeed report```---# 8. Incident Review / Path Replay## What exists`PathReplayView` is a real full view mode. Studio mode switching maps `"replay"` to `PathReplayView`. Root has `openPathReplay` that launches replay mode and sets timeline bottom tab. ## What is goodThe product has a direct Incident Review surface candidate.## What is poorThe product language still says “Path Replay” in mode labels. That is not wrong, but it should be nested under a larger product concept:```textIncident Review / Path Replay```The app should avoid making this feel like animation playback only. It must show:* route state* visibility state* camera responsibility* lost/visible transitions* cause* fix handoff## Needed correctionRename or frame the mode as:```textIncident ReviewPath ReplayVisibility Timeline```The recording path/product walkthrough should say this is for defensive coverage review.---# 9. Counterfactual Fix / Compare## What exists`CompareView` is a real view mode. Root has `openCompareFixes`, which launches compare view and `beforeafter` tab. ## What is goodCompare is reachable and has a dedicated workspace mode.## What is poorRoot label says simply “Compare / Before After.” That can sound like visual comparison rather than verified counterfactual testing.The product concept should be:```textCounterfactual FixBefore / After Verification```## Needed correctionCompare must show:* baseline* proposed fix* what changed* verified simulation deltas* apply/discard* report linkageNo decorative deltas. Everything must tie to scene snapshot/simulation output.---# 10. Audit Report## What exists`ReportView` is a real view mode in `StudioShell`. Root has `openReport`, which launches `viewMode: "report"`, workspace preset `report`, bottom tab `report`. `apps/studio/package.json` depends on `@sentineltwin/report`, `jspdf`, and `jspdf-autotable`, so there is a real report/export foundation. ## What is goodReport is not just theoretical. It has a view and package support.## What is poorThe UI language still says `Report Lite` in workspace modes. For full product readiness, that undermines the final product output. It makes the report sound like a temporary utility.## Needed correctionProduct language should be:```textAudit ReportSecurity Audit ReportReport BuilderReport Export```If there is a compact Studio tab, that can be “Report Summary,” but the product-level endpoint should not be “Lite.”---# 11. Projects / Sites / Reference Sites / Settings## What existsProduct Home has nav labels for Projects, Reference Sites, Reports, Docs, Settings. `SiteIntakeHub` also has nav labels:```textCreate Site TwinWorkspacesProjectsReportsIssues & ActionsEvidenceIntegrations```## What is goodThe product surface acknowledges the full product categories.## What is poorThese nav items are mostly passive or route to generic callbacks. In `SiteIntakeHub`, inactive nav items call `props.onShowProjects`, not proper product routes. This makes the product look wider than it behaves.## Needed correctionEither:1. make these real product views, or2. mark them as unavailable/coming later in a polished way, or3. remove them from primary navigation until they behave.For full app readiness, at minimum:```textProduct HomeCreate Site TwinSecurity Twin StudioAudit ReportReference SitesSettings```should be coherent.---# 12. Visual system / consistency## What existsSome components use CSS variables like:```textvar(--st-border)var(--st-muted)var(--st-text)var(--st-accent)var(--st-panel)```But many feature surfaces still use hardcoded Tailwind values:```textbg-[#08101a]border-white/8text-slate-300bg-sky-500/12rounded-[28px]shadow-[0_24px_80px_rgba(...)]```Examples appear heavily in both Product Home and Site Intake.   ## What is goodThe design direction is coherent: dark operational interface, blue/green/amber security tones, rounded panels, map-heavy product feel.## What is poorEach surface looks hand-styled. Product Home, Site Intake, and Scan Wizard do not yet feel like one system.## Needed correctionBefore pixel alignment, normalize:```textsurfacebordercardbuttonbadgestatus pilltitle scalepanel paddingsidebar widthtop bar heightrisk colorsquality colorsmap colors```Do not begin by tweaking individual screens. Build shared primitives and tokens, then apply them.---# Feature priority order## Immediate blockers1. Root/product view architecture.2. Root first-load state.3. Site Intake and Scan as full product views, not overlays.4. Consistent Site Twin draft review path.5. Product language cleanup: remove “Report Lite” as product endpoint, reduce “workspace” overuse.## Strong existing foundations1. StudioShell is strong.2. SiteIntakeHub has good source taxonomy.3. ScanSiteWizard has real manual-assisted functionality.4. Report infrastructure exists.5. Camera View / Wall / Replay / Compare are real modes.## Main riskThe app already has many parts, but without product architecture cleanup, agents will keep polishing separate surfaces and the product will feel like stitched-together screens.Correct next task:```textRefactor product flow first.Then stabilize root.Then integrate Site Intake / Scan / Draft Review.Then align Studio and report outputs.Then visual-system/pixel alignment.```This is the right order because otherwise the team will keep polishing modal wrappers and static nav that should not exist in the final product structure.