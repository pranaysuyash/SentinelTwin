import StudioShell from "@/components/layout/StudioShell";

// Camera Studio — opens directly into the loaded testbed. No landing page.
export default function StudioPage() {
  return <StudioShell />;
}
