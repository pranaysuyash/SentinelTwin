/**
 * @deprecated This file is preserved for backward compatibility with existing imports.
 * New code should import from `@sentineltwin/core` directly.
 *
 * Resolution path: Q-042 (b) — re-export shim. When all imports are migrated
 * to @sentineltwin/core, this file can be deleted.
 */
export * from "@sentineltwin/core";
